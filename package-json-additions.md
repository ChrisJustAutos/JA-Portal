# Package.json additions needed

Add this line to the `dependencies` section of your `package.json`:

```json
"@react-pdf/renderer": "^4.5.1"
```

Then run `npm install` locally before committing.

Or run directly:
```
npm install @react-pdf/renderer@^4.5.1
```
