{
  "name": "ja-freightbay",
  "version": "1.0.0",
  "private": true,
  "description": "Freight Bay alert bridge — Hikvision NVR line-crossing → ring a Yealink + Slack snapshot. Runs on the FreePBX sync host.",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "probe": "node index.js --probe",
    "test-ring": "node index.js --test-ring",
    "test-snapshot": "node index.js --test-snapshot",
    "test-slack": "node index.js --test-slack"
  },
  "dependencies": {
    "dotenv": "^16.4.5"
  },
  "engines": {
    "node": ">=18"
  }
}
